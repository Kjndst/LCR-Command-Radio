# LLB Command Radio — Agent Binding

Universal shared policy is canonical at:

`D:\Project\UNIVERSAL_PROJECT_GOVERNANCE.md`

Do **not** load that full file for routine coding. For normal work read, in order:

1. `README.md` — durable goal, architecture, roadmap and non-goals.
2. `CURRENT_STATE.md` — current phase, verified facts, blockers and next gate.
3. Only the source/scripts directly relevant to the active task.

Open the universal governance only when the task is about governance/continuity, hygiene/tooling/runtime policy, cross-project safety, or when a conflict cannot be resolved from this repository.

Project invariants:

- Repository evidence is the project source of truth; chat and generated artifacts are not authority.
- REUSE/EXTEND before ADD. Do not create parallel governance, roadmap, status, handoff, evidence or control-plane systems.
- Do not add top-level folders or durable docs without explicit user approval when an existing surface can serve the need.
- Generated server trees, binaries, ZIPs, logs, Docker artifacts and temporary evidence are non-authoritative and must stay ignored/transient.
- Never recursively consume generated outputs as canonical source inputs.
- Lead UX stays lightweight: install one helper, pair once, choose Radio key, use/uninstall normally. Admin owns roles, bots, channels, tokens and host deployment.
- No Discord self-bot or user-token automation. Client never captures microphone audio or logs into Discord.
- Default local voice is Free Mic / Discord Voice Activity. Default command uplink is Hold + Mouse5. Gate fails closed.
- Primary bot-host target is x86_64 Linux + Docker. Start real Discord validation with 1 owner + 1 speaker before scaling to 3–4 speakers.
- Do not auto-next, push, merge, deploy, expose the radio API publicly, or change the locked product concept without explicit user approval.
- Before mutation, verify repository root, branch/HEAD and dirty state. Do not overwrite dirty generated/upstream source blindly.

Keep responses and evidence concise. Update `CURRENT_STATE.md` instead of creating new status/handoff files when the current state changes.
